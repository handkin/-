prec = {
    '*':3, '/':3,
    '+':2, '-':2,
    '(':1
}



Calculator = function(prefix) {
    var array = prefix.split(' ');
    var stack = new Array();
    var postfix = new Array();
    for(i in array)
}



function response(room, msg, sender, isGroupChat, replier, imageDB) {



    msg = msg.trim();
    if(msg.indexOf('계산')!=0)  return;
    msg = msg.replace('계산').trim();
    
}